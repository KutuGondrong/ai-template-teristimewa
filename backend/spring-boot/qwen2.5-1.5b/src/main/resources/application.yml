server:
  port: 8000
spring:
  application:
    name: local-ai-ai_qwen25_1_5b
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://127.0.0.1:5432/ai_qwen25_1_5b}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  sql:
    init:
      mode: always
app:
  ollama-base-url: ${OLLAMA_BASE_URL:http://127.0.0.1:11434}
  ollama-model: ${OLLAMA_MODEL:qwen2.5:1.5b}
  cookie-secure: ${COOKIE_SECURE:false}

springdoc:
  swagger-ui:
    path: /docs
    enabled: true
  api-docs:
    enabled: true
